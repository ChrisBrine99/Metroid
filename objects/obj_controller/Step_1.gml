// Calling the begin_step event for any global structs/objects that contain that event as a function.
with(CAMERA)				{begin_step();}
with(GAME_MANAGER)			{begin_step();}
with(SHADER_OUTLINE)		{begin_step();}