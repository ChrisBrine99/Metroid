// Call the general entity function for the "End Step" event that updates the positions of each active component
// currently attached to the entity relative to its position and any offsets that may exist.
entity_end_step();